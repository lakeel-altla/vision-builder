<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/app_theme_primary</item>
        <item name="colorPrimaryDark">@color/app_theme_primary_dark</item>
        <item name="colorAccent">@color/app_theme_accent</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ArTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/ar_theme_primary</item>
        <item name="colorPrimaryDark">@color/ar_theme_primary_dark</item>
        <item name="colorAccent">@color/ar_theme_accent</item>
        <item name="colorControlNormal">@color/foreground_overlay</item>
        <item name="colorControlActivated">@color/foreground_overlay</item>
        <item name="colorControlHighlight">@color/foreground_overlay</item>
    </style>

</resources>
